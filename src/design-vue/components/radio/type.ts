import { DefaultInputExpose } from '../../types/antdv'
import { DefaultSlots } from '../../types/vue'

// 补充 a-radio 的 slots 类型
export type ARadioSlots = Readonly<DefaultSlots>

// 补充 a-radio 的 expose 类型
export type ARadioExpose = DefaultInputExpose

// 补充 a-radio-group 的 slots 类型
export type ARadioGroupSlots = Readonly<DefaultSlots>
