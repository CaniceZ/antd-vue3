// import { VNode } from 'vue'
// import { PropsType } from '../../types/ts-helpers'
// import { DefaultSlots } from '../../types/vue'

export type Lodop = {
  state: number
  msg?: string
  LODOP?: any
  lodopExe?: string
  needCLodop?: boolean
  LoadJsState?: string
}
